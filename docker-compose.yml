services:
  neo4j:
    image: neo4j:5
    container_name: pramanagst-neo4j
    ports:
      - "7474:7474"   # browser
      - "7687:7687"   # bolt
    environment:
      NEO4J_AUTH: neo4j/pramanagst
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data:
